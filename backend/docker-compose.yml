services:
  backend:
    image: registry.gitlab.com/abdulaziz1999/hujjat/backend:latest
    container_name: hujjat-backend
    ports:
      - '4000:4000'
    env_file:
      - .env
    restart: always
